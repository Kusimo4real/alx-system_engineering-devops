Steps followed to push my file using sftp
1. I logged in using sftp command in my local machine using the ssh hostname and username
2.It required a password, so I input the SSH password and submitted.
3. I printed my local terminal working directory using lpwd and then navigated to where I can see my files(I used command line, because that is the way Ican nvaigate to where my files are)
4. After that I using the command 'put filename and remote path' to push to the remote machine
5. After that I pushed to my github.
